type Query {
	chatroom(chatroom: ID!): Chatroom!
}

type Mutation {
	createChatroom(users: [ID!]!): Chatroom!
	joinChatroom(chatroom: ID!): Chatroom!
	sendMessage(chatroom: ID!, message: String!): Message!
}

type Subscription {
	newMessage(chatroom: ID!): Message!
}
